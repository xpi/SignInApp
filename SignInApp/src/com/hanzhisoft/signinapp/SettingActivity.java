package com.hanzhisoft.signinapp;import java.util.ArrayList;import com.hanzhisoft.signinapp.data.SignInDataSource;import com.hanzhisoft.signinapp.user.UserDataSource;import android.app.Activity;import android.content.Intent;import android.os.Bundle;import android.view.MenuItem;import android.view.View;import android.widget.Button;import android.widget.Spinner;import android.widget.TextView;public class SettingActivity extends Activity {	public UserDataSource user_state;	public Button logout_btn;	public TextView user_name_tv;	// sis.removeAll();	@Override	protected void onCreate(Bundle savedInstanceState) {		// TODO Auto-generated method stub		super.onCreate(savedInstanceState);		setContentView(R.layout.setting_layout);		user_state = new UserDataSource(SettingActivity.this);		logout_btn = (Button) findViewById(R.id.logout_btn);		user_name_tv = (TextView) findViewById(R.id.user_name_tv);		user_name_tv.setText(user_state.getUser().getUserName());		logout_btn.setOnClickListener(new View.OnClickListener() {			@Override			public void onClick(View v) {				user_state.logout();				Intent intent = new Intent(SettingActivity.this,						LoginActivity.class);				startActivity(intent);				ArrayList<Activity> activities = DesktopActivity.holdingActivity;				for (Activity activity : activities) {					activity.finish();				}				finish();			}		});	}	@Override	public boolean onOptionsItemSelected(MenuItem item) {		// Handle action bar item clicks here. The action bar will		// automatically handle clicks on the Home/Up button, so long		// as you specify a parent activity in AndroidManifest.xml.		int id = item.getItemId();		if (id == android.R.id.home) {			finish();		}		return super.onOptionsItemSelected(item);	}}